{
  "name": "mcp-cors-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal zero-dependency CORS proxy for a browser-based app to reach an MCP server.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
